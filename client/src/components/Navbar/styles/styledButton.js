import styled from "styled-components";

export const StyledButton = styled.div`
  button {
    font-weight: 700;
    font-size: 14px;
    border: none;
    padding: 10px;
    border-radius: 3px;
    cursor: pointer;
  }
`